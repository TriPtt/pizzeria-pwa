name: Test Simple

on:
  push:
  pull_request:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "✅ GitHub Actions fonctionne !"
      
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: List files
        run: ls -la
